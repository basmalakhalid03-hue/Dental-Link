{
  "version": 2,
  "installCommand": "npm install --prefix server && npx prisma generate --schema=server/prisma/schema.prisma && npm install --prefix client",
  "buildCommand": "npm run build --prefix client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/uploads/(.*)", "destination": "/api" },
    { "source": "/((?!api|uploads|assets|.*\\..*).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "includeFiles": "server/prisma/dev.db"
    }
  }
}
